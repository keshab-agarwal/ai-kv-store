\* KVStoreBug.cfg — Bug mode configuration
\* Expected result: Error — counterexample found (linearizability violation).
\* BugMode = TRUE causes reads to potentially return wrong values,
\* which violates LinearizabilityCheck and NoPhantomReads.

SPECIFICATION Spec

CONSTANTS
    Clients = {c1, c2}
    Nodes = {n1, n2, n3}
    Keys = {k1}
    Values = {v1, v2}
    MaxOps = 3
    MaxPhase = 2
    CrashNode = n3
    MaxCrashes = 1
    BugMode = TRUE
    NONE = NONE

INVARIANT Invariant

CHECK_DEADLOCK FALSE
