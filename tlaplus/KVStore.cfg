\* TLC configuration for KVStore — normal mode (no bug)

CONSTANTS
    Clients = {c1, c2}
    Replicas = {r1, r2, r3}
    Keys = {k1, k2}
    Values = {v1, v2}
    MaxOps = 4
    MaxCrashes = 1
    BugMode = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeOK
    SafetyInvariant
